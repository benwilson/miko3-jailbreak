<?xml version="1.0" encoding="utf-8"?>
<!-- Miko 3 custom launcher — proof of concept.
     Replaces the stock home app (com.miko.launcher_app) as the default HOME
     activity. Shows basic device info on-screen and serves the same info as
     a read-only web page over Wi-Fi (see InfoHttpServer). Also offers basic
     Wi-Fi connect/disconnect/forget, since the stock kiosk UI has no
     reachable Wi-Fi settings screen and the device may not be on a network
     at all otherwise.
     Install/restore: scripts/install-custom-launcher.py,
     scripts/restore-stock-launcher.py. See launcher/README.md. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.miko3.launcher"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:label="Miko3 Launcher"
        android:name=".LauncherApp"
        android:allowBackup="false">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.HOME"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
