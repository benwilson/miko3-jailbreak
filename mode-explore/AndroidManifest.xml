<?xml version="1.0" encoding="utf-8"?>
<!-- Miko 3 explore mode: the robot wanders the desk on its own. Launched by
     the launcher via explicit Intent (not HOME) and returns control to it on
     exit, matching the remote-control and voice modes' manifests. No CAMERA
     or RECORD_AUDIO permission: the mode reads the controller's sensor replies
     over the shared motor driver and never opens the camera or microphone.
     See docs/plans/2026-09-22-1438-feat-explore-mode-plan.md. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.miko3.mode.explore"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="Miko3 Explore"
        android:name=".ModeApp"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false">

        <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
