<?xml version="1.0" encoding="utf-8"?>
<!-- Miko 3 voice-conversation mode. Launched by the launcher via explicit
     Intent (not HOME) and returns control to it on exit, matching the
     remote-control mode's manifest. No CAMERA permission: the mode never
     opens the camera and never drives the motors (R18). See
     docs/plans/2026-09-17-1617-feat-autonomous-voice-mode-plan.md. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.miko3.mode.voice"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:label="Miko3 Voice"
        android:name=".ModeApp"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false">

        <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
