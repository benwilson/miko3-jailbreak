# Firmware

Images are **not** committed — they are large and device-specific. Track them
here instead.

| File | Source | Version | sha256 | Notes |
|------|--------|---------|--------|-------|
| _(none yet)_ | | | | |

Record for each image: where it came from (OTA capture, eMMC dump, vendor URL),
the device build fingerprint it belongs to, and its checksum. Store the actual
file in `firmware/` locally or in offline backup.
